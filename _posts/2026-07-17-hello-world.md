---
layout: post
title: "hello, world"
---
First post on the new ASCII site. Everything you see — the nav, the
boxes, the dividers — is drawn in characters.

To write a post, I just add a Markdown file to `_posts/` named
`YYYY-MM-DD-title.md` with a little front matter at the top, push it,
and GitHub does the rest.

```
+----------------------+
| code blocks get      |
| boxes for free, too  |
+----------------------+
```

More soon.
